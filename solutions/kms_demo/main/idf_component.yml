## IDF Component Manager Manifest File
dependencies:
  ## Required IDF version
  idf:
    version: ">=4.1.0"
  ## Import needed components only
  espressif/esp_h264:
    version: "~1.3"
    rules:
    - if: target in [esp32p4, esp32s3]
  esp_peer:
    override_path: ../../../components/esp_peer
  ## Note: kms_signal is implemented locally in kms_signal.c
